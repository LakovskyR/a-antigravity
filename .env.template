# Antigravity — Environment Variables Template
# ─────────────────────────────────────────────────────────────────────────────
# SETUP:
#   1. Copy this file:   copy .env.template .env
#   2. Fill in your keys below in .env
#   3. Never commit .env (it is in .gitignore)
# ─────────────────────────────────────────────────────────────────────────────

# ── LLM API Keys (at least one required) ─────────────────────────────────────
OPENAI_API_KEY=sk-...
CLAUDE_API_KEY=sk-ant-...
KIMI_API_KEY=
PERPLEXITY_API_KEY=

# ── Microsoft SharePoint — Data loading ──────────────────────────────────────
SHAREPOINT_CLIENT_ID=
SHAREPOINT_SECRET=
SHAREPOINT_TENANT_ID=
ANTIGRAVITY_DATA_MODE=codes

# ── Microsoft SharePoint — Analyst Feedback Writeback ────────────────────────
# How to get these values: see DEPLOYMENT.md → "SharePoint Feedback Setup"
#
# SHAREPOINT_FEEDBACK_SITE_ID   : your SharePoint site ID
#   Find it: GET https://graph.microsoft.com/v1.0/sites/{tenant}.sharepoint.com:/sites/{sitename}
#
# SHAREPOINT_FEEDBACK_FOLDER    : path inside the site drive where feedback.md lives
#   Example: Shared Documents/Antigravity/feedback
#
# SHAREPOINT_FEEDBACK_FILE      : name of the markdown file to append rows to
#   Example: analyst_feedback.md
#
SHAREPOINT_FEEDBACK_SITE_ID=
SHAREPOINT_FEEDBACK_FOLDER=Shared Documents/Antigravity/feedback
SHAREPOINT_FEEDBACK_FILE=analyst_feedback.md

# ── GitHub (optional — for automated push) ───────────────────────────────────
GITHUB_TOKEN=ghp_...
GITHUB_REPO=https://github.com/LakovskyR/antigravity

# ── Pinecone Vector DB (optional) ────────────────────────────────────────────
PINECONE_API_KEY=
PINECONE_ENVIRONMENT=
